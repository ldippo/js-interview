/**
 * Given an array of cats and optionally a breed,
 * write a function that returns an array of kitten names, sorted from youngest to oldest.
 * If breed is included as a parameter only return kittens of that breed.
 * Assume that cats are kittens if they are below one year of age.
 * @param cats array of cat objects shaped { name: string, age: string (months), breed: string }
 * @param breed string (optional)
 */
export default function getKittens(cats, breed) {
  return null;
}
